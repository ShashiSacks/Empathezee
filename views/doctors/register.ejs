<%- include('../includes/header', { title: 'Doctor Register', bodyClass: 'auth-page' }) %>

<div class="auth-container">

    <!-- left side (form area) -->
    <div class="auth-left" style="overflow-y: auto; padding-top: 60px; padding-bottom: 60px;">
        <div class="auth-left-content">
            <h1 class="title">Doctor Register</h1>

            <form action="/api/auth/doctor/register" method="POST">

                <input type="text" name="username" placeholder="Full name" required autocomplete="name" />

                <input type="email" name="email" placeholder="Doctor email" required autocomplete="email" />

                <input type="password" name="password" placeholder="Password" required autocomplete="new-password" />

                <input type="number" name="age" placeholder="Age" min="18" max="120" />

                <div class="form-group">
                    <label for="gender">Gender</label>
                    <select name="gender" id="gender" required>
                        <option value="" disabled selected>Select Gender</option>
                        <option value="male">Male</option>
                        <option value="female">Female</option>
                        <option value="other">Rather not to say</option>
                    </select>
                </div>

                <input type="text" name="disease" placeholder="Specialization" />

                <!-- cascading location picker -->
                <div class="location-picker">
                    <div class="form-group">
                        <label for="country">Country <span class="optional-tag">(optional)</span></label>
                        <select name="country" id="country">
                            <option value="">Select Country</option>
                        </select>
                    </div>

                    <div class="form-group" id="state-group" style="display: none;">
                        <label for="state">State <span class="optional-tag">(optional)</span></label>
                        <select id="state">
                            <option value="">Select State</option>
                        </select>
                        <input type="text" id="state-input" placeholder="Type State" style="display: none;" />
                    </div>

                    <div class="form-group" id="district-group" style="display: none;">
                        <label for="district">District <span class="optional-tag">(optional)</span></label>
                        <select id="district">
                            <option value="">Select District</option>
                        </select>
                        <input type="text" id="district-input" placeholder="Type District" style="display: none;" />
                    </div>

                    <div class="form-group" id="city-group" style="display: none;">
                        <label for="city">City / Place <span class="optional-tag">(optional)</span></label>
                        <select id="city">
                            <option value="">Select City</option>
                        </select>
                        <input type="text" id="city-input" placeholder="Type City / Place" style="display: none;" />
                    </div>
                </div>

                <textarea name="bio" placeholder="Short professional bio"></textarea>

                <button type="submit">Create Doctor Account</button>

            </form>

            <p>
                Already registered?
                <a href="/doctor/login">Doctor login</a>
            </p>

            <p>
                Patient account?
                <a href="/register">Patient registration</a>
            </p>
        </div>
    </div>

    <!-- right side image -->
    <div class="auth-right">
        <div class="auth-right-content-wrapper">
            <div class="auth-right-logo">
                <span class="auth-logo-icon"><i class="fa-solid fa-heart-pulse"></i></span>
                <span class="auth-logo-text">Empathezee Pro</span>
            </div>
            <h2 class="auth-right-title">Empower patients, guide support groups.</h2>
            <p class="auth-right-subtitle">Join our verified network of medical specialists. Moderate discussions, host online consultations, and help patients manage chronic conditions.</p>
            <div class="auth-right-stats">
                <div class="auth-stat-item">
                    <span class="auth-stat-num">24/7</span>
                    <span class="auth-stat-lbl">Care Guidance</span>
                </div>
                <div class="auth-stat-item">
                    <span class="auth-stat-num">15k+</span>
                    <span class="auth-stat-lbl">Patient Interactions</span>
                </div>
                <div class="auth-stat-item">
                    <span class="auth-stat-num">100%</span>
                    <span class="auth-stat-lbl">Verified Specialists</span>
                </div>
            </div>
        </div>
    </div>

</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    var countryEl = document.getElementById('country');
    var stateEl = document.getElementById('state');
    var districtEl = document.getElementById('district');
    var cityEl = document.getElementById('city');

    var stateInput = document.getElementById('state-input');
    var districtInput = document.getElementById('district-input');
    var cityInput = document.getElementById('city-input');

    function configureField(prefix, mode) {
        var group = document.getElementById(prefix + '-group');
        var select = document.getElementById(prefix);
        var input = document.getElementById(prefix + '-input');

        if (mode === 'select') {
            group.style.display = 'flex';
            select.style.display = 'block';
            select.setAttribute('name', prefix);
            input.style.display = 'none';
            input.removeAttribute('name');
        } else if (mode === 'input') {
            group.style.display = 'flex';
            select.style.display = 'none';
            select.removeAttribute('name');
            input.style.display = 'block';
            input.setAttribute('name', prefix);
        } else {
            group.style.display = 'none';
            select.removeAttribute('name');
            input.removeAttribute('name');
        }
    }

    // populate countries
    var countries = LOCATION_DATA.getCountries();
    countries.forEach(function(c) {
        var opt = document.createElement('option');
        opt.value = c;
        opt.textContent = c;
        countryEl.appendChild(opt);
    });

    // country change → populate states (if india)
    countryEl.addEventListener('change', function() {
        var country = this.value;
        stateEl.innerHTML = '<option value="">Select State</option>';
        districtEl.innerHTML = '<option value="">Select District</option>';
        cityEl.innerHTML = '<option value="">Select City</option>';

        stateInput.value = '';
        districtInput.value = '';
        cityInput.value = '';

        if (!country) {
            configureField('state', 'none');
            configureField('district', 'none');
            configureField('city', 'none');
        } else if (LOCATION_DATA.hasDetailedData(country)) {
            var states = LOCATION_DATA.getStates(country);
            states.forEach(function(s) {
                var opt = document.createElement('option');
                opt.value = s;
                opt.textContent = s;
                stateEl.appendChild(opt);
            });
            configureField('state', 'select');
            configureField('district', 'none');
            configureField('city', 'none');
        } else {
            configureField('state', 'input');
            configureField('district', 'input');
            configureField('city', 'input');
        }
    });

    // state change → populate districts
    stateEl.addEventListener('change', function() {
        var state = this.value;
        districtEl.innerHTML = '<option value="">Select District</option>';
        cityEl.innerHTML = '<option value="">Select City</option>';
        districtInput.value = '';
        cityInput.value = '';

        if (state) {
            var districts = LOCATION_DATA.getDistricts(state);
            districts.forEach(function(d) {
                var opt = document.createElement('option');
                opt.value = d;
                opt.textContent = d;
                districtEl.appendChild(opt);
            });
            configureField('district', 'select');
            configureField('city', 'none');
        } else {
            configureField('district', 'none');
            configureField('city', 'none');
        }
    });

    // district change → populate cities
    districtEl.addEventListener('change', function() {
        var district = this.value;
        cityEl.innerHTML = '<option value="">Select City</option>';
        cityInput.value = '';

        if (district) {
            var cities = LOCATION_DATA.getCities(district);
            cities.forEach(function(c) {
                var opt = document.createElement('option');
                opt.value = c;
                opt.textContent = c;
                cityEl.appendChild(opt);
            });
            configureField('city', 'select');
        } else {
            configureField('city', 'none');
        }
    });
});
</script>

<%- include('../includes/footer') %>
